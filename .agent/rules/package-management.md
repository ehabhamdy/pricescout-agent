---
trigger: always_on
---

This project rely on uv for package management. When trying to run a command please make sure to:
1. Activate the virtual environment using `source .venv/bin/activate`
2. To install a dependency please use `uv add`
3. Please make sure to use all relevant uv commands as needed.